---
name: github
description: github integration via Composio MCP. Use when user needs to interact with github.
---

# github Integration

> Auto-generated by Composio Skills Extension on 2026-05-31T12:44:31.295Z
> **Execution**: Via Composio MCP Server (native Claude Code integration)

## Overview

Integration with github services.

This skill uses **Composio MCP** for tool execution. Tools are available as native MCP tools
that Claude can call directly without any Bash commands or scripts.

## MCP Configuration

This integration requires Composio MCP server to be configured in `.mcp.json`:

```json
{
  "mcpServers": {
    "composio": {
      "command": "npx",
      "args": ["-y", "@composio/mcp"],
      "env": {
        "COMPOSIO_API_KEY": "your_api_key",
        "COMPOSIO_ENTITY_ID": "your_entity_id"
      }
    }
  }
}
```

## Available MCP Tools

### GITHUB_ABORT_REPOSITORY_MIGRATION

**MCP Tool**: `mcp__composio__GITHUB_ABORT_REPOSITORY_MIGRATION`

Tool to abort a repository migration that is queued or in progress. Use when you need to cancel an ongoing migration operation.

**Parameters:**
  - `migrationId` (string, required): The ID of the repository migration to abort. This is the migration ID returned when a migration was queued (e.g., 'RM_kgDOABCDEF').
  - `clientMutationId` (string, optional): A unique identifier for the client performing the mutation. This can be used to track the mutation in logs or for idempotency purposes.

### GITHUB_ACCEPT_REPOSITORY_INVITATION

**MCP Tool**: `mcp__composio__GITHUB_ACCEPT_REPOSITORY_INVITATION`

Accepts a PENDING repository invitation that has been issued to the authenticated user.

**Parameters:**
  - `invitation_id` (integer, required): Unique identifier of the repository invitation sent TO the authenticated user. Get this ID by listing your pending invitations using the 'List repository invitations for the authenticated user' action.

### GITHUB_ADD_APP_ACCESS_RESTRICTIONS

**MCP Tool**: `mcp__composio__GITHUB_ADD_APP_ACCESS_RESTRICTIONS`

Adds GitHub Apps to the list of apps allowed to push to a protected branch. The branch must already have protection rules with restrictions enabled. This endpoint only works for organization repositories, not personal repositories. Apps must be installed on the repository with 'contents' write permissions.

**Parameters:**
  - `apps` (array, required): The list of GitHub App slugs with push access to grant. Apps must be installed on the repository and have 'contents' write permissions. The app slug is the URL-friendly name of the GitHub App (e.g., 'github-actions', 'dependabot').
  - `repo` (string, required): The name of the repository, without the `.git` extension. This field is not case-sensitive.
  - `owner` (string, required): The organization name that owns the repository. This field is not case-sensitive. Note: This endpoint only works for organization-owned repositories.
  - `branch` (string, required): The name of the branch to which restrictions will be applied. Wildcard characters (e.g., '*') are not allowed in the branch name when using this REST API endpoint. For operations involving wildcard branch names, consult the GitHub GraphQL API.

### GITHUB_ADD_A_REPOSITORY_COLLABORATOR

**MCP Tool**: `mcp__composio__GITHUB_ADD_A_REPOSITORY_COLLABORATOR`

Adds a GitHub user as a repository collaborator, or updates their permission if already a collaborator; `permission` applies to organization-owned repositories (personal ones default to 'push' and ignore this field), and an invitation may be created or permissions updated directly.

**Parameters:**
  - `repo` (string, required): The name of the repository, without the `.git` extension. This name is not case sensitive.
  - `owner` (string, required): The account owner of the repository. This name is not case sensitive.
  - `username` (string, required): The GitHub handle for the user account to add as a collaborator. Cannot be the same as the repository owner, as owners are automatically collaborators.
  - `permission` (string, optional): Permission level for the collaborator. For organization-owned repositories, valid values are `pull` (read-only), `triage` (manage issues/PRs), `push` (read/write), `maintain` (manage most settings), `admin` (full control), or a custom repository role name if defined by the owning organization. This parameter is ignored for personal repositories, which grant `push` access.

### GITHUB_ADD_ASSIGNEES_TO_AN_ISSUE

**MCP Tool**: `mcp__composio__GITHUB_ADD_ASSIGNEES_TO_AN_ISSUE`

Adds assignees to a GitHub issue. This action only adds users - it does not remove existing assignees. Changes are silently ignored if the authenticated user lacks push access to the repository.

**Parameters:**
  - `repo` (string, required): The name of the repository, without the `.git` extension. This field is not case sensitive.
  - `owner` (string, required): The username of the account or the organization name that owns the repository. This field is not case sensitive.
  - `assignees` (array, optional): A list of GitHub usernames to add as assignees to the issue. Up to 10 assignees total can be on an issue. NOTE: This endpoint ADDS assignees - it does not replace existing ones. Users already assigned are kept. To remove assignees, use the 'remove_assignees_from_an_issue' action instead. The authenticated user must have push access to the repository; otherwise changes are silently ignored.
  - `issue_number` (integer, required): The unique number identifying the issue within the repository. Must be a positive integer (GitHub issue numbers start at 1).

### GITHUB_ADD_EMAIL_ADDRESS_FOR_AUTHENTICATED_USER

**MCP Tool**: `mcp__composio__GITHUB_ADD_EMAIL_ADDRESS_FOR_AUTHENTICATED_USER`

Adds one or more email addresses (which will be initially unverified) to the authenticated user's GitHub account; use this to associate new emails, noting an email verified for another account will error, while an existing email for the current user is accepted.

**Parameters:**
  - `emails` (array, required): Array of email addresses to add to the authenticated user account. Must contain at least one email address.

### GITHUB_ADD_FIELD_TO_USER_PROJECT

**MCP Tool**: `mcp__composio__GITHUB_ADD_FIELD_TO_USER_PROJECT`

Tool to add a custom field to a user-owned GitHub Projects V2 project. Use when you need to add fields like status, priority, or custom data to organize project items.

**Parameters:**
  - `name` (string, required): The name of the field to create.
  - `username` (string, required): The handle for the GitHub user account.
  - `data_type` (string, required): The data type of the field. Use 'single_select' for dropdown fields, 'iteration' for sprint fields, or other types for simple fields.
  - `project_number` (integer, required): The project's number.
  - `single_select_options` (array, optional): Options for single select fields. Required when data_type is 'single_select'.
  - `iteration_configuration` (object, optional): Configuration for iteration fields.

### GITHUB_ADD_ITEM_TO_USER_PROJECT

**MCP Tool**: `mcp__composio__GITHUB_ADD_ITEM_TO_USER_PROJECT`

Tool to add an issue or pull request to a user-owned GitHub project. Use when you need to add existing repository items to a project board.

**Parameters:**
  - `id` (integer, optional): The unique identifier of the issue or pull request to add to the project. Required if owner, repo, and number are not provided.
  - `repo` (string, optional): The repository name. Required with owner and number if id is not provided.
  - `type` (string, required): The type of item to add to the project. Must be either Issue or PullRequest.
  - `owner` (string, optional): The repository owner login. Required with repo and number if id is not provided.
  - `number` (integer, optional): The issue or pull request number. Required with owner and repo if id is not provided.
  - `username` (string, required): The handle for the GitHub user account.
  - `project_number` (integer, required): The project's number.

### GITHUB_ADD_LABELS_TO_AN_ISSUE

**MCP Tool**: `mcp__composio__GITHUB_ADD_LABELS_TO_AN_ISSUE`

Adds labels (provided in the request body) to a repository issue; labels that do not already exist are created.

**Parameters:**
  - `repo` (string, required): Repository name, without the `.git` extension (case-insensitive).
  - `owner` (string, required): Username or organization name owning the repository (case-insensitive).
  - `labels` (array, required): Array of label names to add to the issue. Labels that don't exist will be created automatically.
  - `issue_number` (integer, required): Positive integer that uniquely identifies the issue within the repository. Must be >= 1.

### GITHUB_ADD_ORG_RUNNER_LABELS

**MCP Tool**: `mcp__composio__GITHUB_ADD_ORG_RUNNER_LABELS`

Adds new custom labels to an existing self-hosted runner for an organization; existing labels are not removed, and duplicates are not added.

**Parameters:**
  - `org` (string, required): The organization's name (case-insensitive).
  - `labels` (array, required): A list of custom label names to add to the self-hosted runner. Previously applied labels are not removed.
  - `runner_id` (integer, required): Unique identifier of the self-hosted runner.

### GITHUB_ADD_OR_UPDATE_TEAM_MEMBERSHIP_FOR_USER

**MCP Tool**: `mcp__composio__GITHUB_ADD_OR_UPDATE_TEAM_MEMBERSHIP_FOR_USER`

Adds a GitHub user to a team or updates their role (member or maintainer), inviting them to the organization if not already a member; idempotent, returning current details if no change is made.

**Parameters:**
  - `org` (string, required): The name of the GitHub organization. This name is not case-sensitive.
  - `role` (string, optional): The role to assign to the user within the team. A 'maintainer' has broader permissions than a 'member', such as managing team members and settings.
  - `username` (string, required): The GitHub username of the user to add or whose membership to update.
  - `team_slug` (string, required): The slug (URL-friendly version) of the team's name. Must be lowercase with hyphens instead of spaces (e.g., 'my-team' not 'My Team'). Spaces will be automatically converted to hyphens and text will be lowercased.

### GITHUB_ADD_OR_UPDATE_TEAM_PROJECT_PERMISSIONS

**MCP Tool**: `mcp__composio__GITHUB_ADD_OR_UPDATE_TEAM_PROJECT_PERMISSIONS`

Adds a classic project to a team or updates the team's permission on it. This endpoint grants or updates permissions for a team on a specific classic project (not Projects V2). The authenticated user must have admin permissions for the project. Both the team and project must belong to the same organization. Requirements: - The project must be a classic project (not GitHub Projects V2) - The authenticated user must have admin permissions on the project - The team and project must be in the same organization - Requires 'admin:org' scope for the authentication token Returns HTTP 204 No Content on success, 403 if project is not an org project, or 404 if the organization, team, or project is not found.

**Parameters:**
  - `org` (string, required): The organization name (login) that owns both the team and the classic project. Case-insensitive.
  - `team_slug` (string, required): The team's URL-friendly slug identifier within the organization. Found in the team's URL path.
  - `permission` (string, optional): Permission level to grant the team: 'read' (view only), 'write' (view and edit), or 'admin' (full control including settings). If omitted, uses the team's default permission.
  - `project_id` (integer, required): The unique numeric ID of the classic project. This is NOT the project number displayed in the UI.

### GITHUB_ADD_OR_UPDATE_TEAM_REPOSITORY_PERMISSIONS

**MCP Tool**: `mcp__composio__GITHUB_ADD_OR_UPDATE_TEAM_REPOSITORY_PERMISSIONS`

Sets or updates a team's permission level for a repository within an organization; the team must be a member of the organization.

**Parameters:**
  - `org` (string, required): Organization name where the team exists (case-insensitive). This is the organization that owns the team, not necessarily the repository owner.
  - `repo` (string, required): Repository name, without the `.git` extension (case-insensitive).
  - `owner` (string, required): Account owner of the repository (case-insensitive). This is the user or organization that owns the repository. In most cases, this should match the 'org' parameter value.
  - `team_slug` (string, required): The team's slug (URL-friendly name).
  - `permission` (string, optional): Permission level to grant the team. Valid values: 'pull' (read-only), 'triage' (manage issues/PRs), 'push' (read/write), 'maintain' (manage repo settings plus push), 'admin' (full control). Custom repository role names are also supported if defined by the organization.

### GITHUB_ADD_PROJECT_COLLABORATOR

**MCP Tool**: `mcp__composio__GITHUB_ADD_PROJECT_COLLABORATOR`

Adds a specified GitHub user as a collaborator to an existing organization project with a given permission level. Note: This endpoint is for organization projects (classic). You must be an organization owner or a project admin to add a collaborator. Classic projects are being deprecated in favor of the new Projects experience.

**Parameters:**
  - `username` (string, required): The GitHub username of the user to be added as a collaborator.
  - `permission` (string, optional): Permission level for the collaborator on the project.
  - `project_id` (integer, required): The unique identifier of the organization project.

### GITHUB_ADD_REPOSITORY_TO_APP_INSTALLATION

**MCP Tool**: `mcp__composio__GITHUB_ADD_REPOSITORY_TO_APP_INSTALLATION`

Adds a repository to a GitHub App installation, granting the app access; requires authenticated user to have admin rights for the repository and access to the installation.

**Parameters:**
  - `repository_id` (integer, required): The unique identifier of the repository to add to the installation. The authenticated user must have admin permissions for this repository.
  - `installation_id` (integer, required): The unique identifier of the GitHub App installation to which the repository will be added. This installation must be accessible to the authenticated user.

### GITHUB_ADD_REPO_TO_ORG_SECRET_WITH_SELECTED_ACCESS

**MCP Tool**: `mcp__composio__GITHUB_ADD_REPO_TO_ORG_SECRET_WITH_SELECTED_ACCESS`

Adds a repository to an existing organization-level GitHub Actions secret that is configured for 'selected' repository access.

**Parameters:**
  - `org` (string, required): The name of the GitHub organization. This field is not case-sensitive.
  - `secret_name` (string, required): The name of the organization secret to which the repository will be added.
  - `repository_id` (integer, required): The unique identifier of the repository to be granted access to the organization secret.

### GITHUB_ADD_REPO_TO_ORG_SECRET_WITH_SELECTED_VISIBILITY

**MCP Tool**: `mcp__composio__GITHUB_ADD_REPO_TO_ORG_SECRET_WITH_SELECTED_VISIBILITY`

Grants an existing repository access to an existing organization-level Dependabot secret when the secret's visibility is set to 'selected'; the repository must belong to the organization, and the call succeeds without change if access already exists.

**Parameters:**
  - `org` (string, required): The organization's unique handle/name (case-insensitive).
  - `secret_name` (string, required): Name of the Dependabot secret; must be alphanumeric or underscore, no spaces (lowercase auto-converts to uppercase).
  - `repository_id` (integer, required): Unique ID of the repository to be granted access to the secret.

### GITHUB_ADD_RUNNER_LABELS

**MCP Tool**: `mcp__composio__GITHUB_ADD_RUNNER_LABELS`

Adds and appends custom labels to a self-hosted repository runner, which must be registered and active.

**Parameters:**
  - `repo` (string, required): Name of the repository, without the `.git` extension. Case-insensitive.
  - `owner` (string, required): Username or organization name of the repository owner. Case-insensitive.
  - `labels` (array, required): Custom label names to add. Appended to existing labels. Names are case-insensitive. Empty list means no changes.
  - `runner_id` (integer, required): Unique ID of the self-hosted runner.

### GITHUB_ADD_SELECTED_REPOSITORY_TO_ORGANIZATION_SECRET

**MCP Tool**: `mcp__composio__GITHUB_ADD_SELECTED_REPOSITORY_TO_ORGANIZATION_SECRET`

Adds a repository to an organization secret's access list when the secret's visibility is 'selected'; this operation is idempotent.

**Parameters:**
  - `org` (string, required): The name of the organization. This name is not case sensitive.
  - `secret_name` (string, required): The name of the secret. Secret names can only contain alphanumeric characters ([A-Z], [a-z], [0-9]) or underscores (_). Spaces are not allowed. Secret names cannot start with GITHUB_ and must be less than 255 characters.
  - `repository_id` (integer, required): The unique identifier of the repository to add to the secret's access list.

### GITHUB_ADD_SELECTED_REPOSITORY_TO_ORGANIZATION_VARIABLE

**MCP Tool**: `mcp__composio__GITHUB_ADD_SELECTED_REPOSITORY_TO_ORGANIZATION_VARIABLE`

Grants a repository access to an organization-level GitHub Actions variable, if that variable's visibility is set to 'selected_repositories'.

**Parameters:**
  - `org` (string, required): Name of the GitHub organization (case-insensitive).
  - `name` (string, required): Name of the existing organization-level GitHub Actions variable.
  - `repository_id` (integer, required): Unique identifier of the repository to grant access.


## Usage Examples

#### GITHUB_ABORT_REPOSITORY_MIGRATION
```
Use MCP tool: mcp__composio__GITHUB_ABORT_REPOSITORY_MIGRATION
Parameters: {
  "migrationId": "your_migrationId_here"
}
```

#### GITHUB_ACCEPT_REPOSITORY_INVITATION
```
Use MCP tool: mcp__composio__GITHUB_ACCEPT_REPOSITORY_INVITATION
Parameters: {
  "invitation_id": "123"
}
```

#### GITHUB_ADD_APP_ACCESS_RESTRICTIONS
```
Use MCP tool: mcp__composio__GITHUB_ADD_APP_ACCESS_RESTRICTIONS
Parameters: {
  "owner": "your_owner_here",
  "repo": "your_repo_here"
}
```

## Quick Reference

| MCP Tool | Description |
|----------|-------------|
| `mcp__composio__GITHUB_ABORT_REPOSITORY_MIGRATION` | Tool to abort a repository migration that is queue... |
| `mcp__composio__GITHUB_ACCEPT_REPOSITORY_INVITATION` | Accepts a PENDING repository invitation that has b... |
| `mcp__composio__GITHUB_ADD_APP_ACCESS_RESTRICTIONS` | Adds GitHub Apps to the list of apps allowed to pu... |
| `mcp__composio__GITHUB_ADD_A_REPOSITORY_COLLABORATOR` | Adds a GitHub user as a repository collaborator, o... |
| `mcp__composio__GITHUB_ADD_ASSIGNEES_TO_AN_ISSUE` | Adds assignees to a GitHub issue. This action only... |
| `mcp__composio__GITHUB_ADD_EMAIL_ADDRESS_FOR_AUTHENTICATED_USER` | Adds one or more email addresses (which will be in... |
| `mcp__composio__GITHUB_ADD_FIELD_TO_USER_PROJECT` | Tool to add a custom field to a user-owned GitHub ... |
| `mcp__composio__GITHUB_ADD_ITEM_TO_USER_PROJECT` | Tool to add an issue or pull request to a user-own... |
| `mcp__composio__GITHUB_ADD_LABELS_TO_AN_ISSUE` | Adds labels (provided in the request body) to a re... |
| `mcp__composio__GITHUB_ADD_ORG_RUNNER_LABELS` | Adds new custom labels to an existing self-hosted ... |
| `mcp__composio__GITHUB_ADD_OR_UPDATE_TEAM_MEMBERSHIP_FOR_USER` | Adds a GitHub user to a team or updates their role... |
| `mcp__composio__GITHUB_ADD_OR_UPDATE_TEAM_PROJECT_PERMISSIONS` | Adds a classic project to a team or updates the te... |
| `mcp__composio__GITHUB_ADD_OR_UPDATE_TEAM_REPOSITORY_PERMISSIONS` | Sets or updates a team's permission level for a re... |
| `mcp__composio__GITHUB_ADD_PROJECT_COLLABORATOR` | Adds a specified GitHub user as a collaborator to ... |
| `mcp__composio__GITHUB_ADD_REPOSITORY_TO_APP_INSTALLATION` | Adds a repository to a GitHub App installation, gr... |
| `mcp__composio__GITHUB_ADD_REPO_TO_ORG_SECRET_WITH_SELECTED_ACCESS` | Adds a repository to an existing organization-leve... |
| `mcp__composio__GITHUB_ADD_REPO_TO_ORG_SECRET_WITH_SELECTED_VISIBILITY` | Grants an existing repository access to an existin... |
| `mcp__composio__GITHUB_ADD_RUNNER_LABELS` | Adds and appends custom labels to a self-hosted re... |
| `mcp__composio__GITHUB_ADD_SELECTED_REPOSITORY_TO_ORGANIZATION_SECRET` | Adds a repository to an organization secret's acce... |
| `mcp__composio__GITHUB_ADD_SELECTED_REPOSITORY_TO_ORGANIZATION_VARIABLE` | Grants a repository access to an organization-leve... |

## When to Use This Integration

**Use github when:**
- Creating issues and PRs
- Managing repositories
- Reading code and commits
- Automating workflows

## Best Practices

1. **Prefer batch operations** when working with multiple items
2. **Check permissions** before performing write operations
3. **Handle errors gracefully** - MCP returns structured error responses
4. **Use appropriate tools** - choose the most specific tool for the task

## Authentication

- **Method**: OAuth via Composio
- **Status**: Connected and managed by Composio
- **Token Refresh**: Automatic

## Troubleshooting

If MCP tools are not available:
1. Check that `.mcp.json` is properly configured
2. Verify Composio API key is valid
3. Ensure the integration is connected in Composio dashboard
4. Restart Claude Code to reload MCP configuration

## Related Skills

Consider using with: Slack (for PR notifications), Linear (for issue sync)
